# Rakkib Agent Notes

Read `AGENT_PROTOCOL.md` first. It is the single normative installer spec for Rakkib.

Do not write outside this repo until `questions/06-confirm.md` records `confirmed: true` in `.fss-state.yaml`. After confirmation, execute the numbered files in `steps/` in order and pass every `## Verify` block before advancing.

Use `bd` for repository task tracking. The detailed beads workflow lives in `docs/beads.md`.
