	# All subdomains
	@subdomains host *.{{DOMAIN}}
	handle @subdomains {
		respond "Rakkib route is configured for {http.request.host}" 200
	}

	# Fallback
	handle {
		respond "Rakkib Caddy is running" 200
	}
}
