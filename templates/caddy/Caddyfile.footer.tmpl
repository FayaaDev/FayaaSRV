	# All subdomains
	@subdomains host *.{{DOMAIN}}
	handle @subdomains {
		respond "Caddy is ready (via Cloudflare tunnel) ✅ Subdomain: {http.request.host}" 200
	}

	# Fallback
	handle {
		respond "Caddy on Beelink" 200
	}
}
