# Cloudflared configuration is stored in tunnel credentials
# Tunnel UUID and credentials are in {{DATA_ROOT}}/data/cloudflared/
# No environment variables needed - credentials handled via volume mount
